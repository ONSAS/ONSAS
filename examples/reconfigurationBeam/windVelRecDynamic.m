function windVel = windVelRecDynamic(x,t)

% load parameters
 [~, ~, ~, ~, ~, ~, ~, uy_vec  ] = loadParamteters() ;
% compute the wind vel vector according to the cy vector
windVel = [0 40 0 ]'; 

end